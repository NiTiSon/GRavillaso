if(JavaVersion.current().ordinal() < JavaVersion.VERSION_16.ordinal()){
    throw new Exception("!!! YOU MUST USE JAVA 16 OR ABOVE TO COMPILE AND RUN GRAVILLASO !!! Your version: ${System.properties["java.version"]}")
}

include "core", "tools"